<div class="jumbotron p-4">
  <h2><b>Distritos</b></h2>
  <p id="notice" class="badge badge-pill badge-danger pt-2 pb-2"><%= notice %></p>
  <div class="table-responsive-sm">
    <table class="table table-striped table-hover w-50">
      <thead class="thead-dark">
        <tr>
          <th colspan="3" class="text-center">
            <%= link_to image_tag('NewIcon.png'), new_city_path, title: 'Nuevo' %>
          </th>
          <th>Idstate</th>
          <th>Nombre</th>
        </tr>
      </thead>

      <tbody>
        <% @cities.each do |city| %>
          <tr>
            <td style="width:33px" class="pr-2 pl-2"><%= link_to image_tag('ShowIcon.png'), city, title: 'Ver' %></td>
            <td style="width:33px" class="pr-1 pl-1"><%= link_to image_tag('EditIcon.png'), edit_city_path(city), title: 'Editar' %></td>
            <td style="width:40px" class="pr-1 pl-2">
              <%= link_to image_tag('DeleteIcon.png'), city, title: 'Eliminar', method: :delete, data: { confirm: '¿Esta seguro?' } %>              
            </td>            
            <td><%= city.IdState %></td>
            <td><%= city.Name %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<br>